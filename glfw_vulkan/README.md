# glfw_vulkan

Reusable Ada 2022 GLFW wrapper focused on Vulkan window creation and event
delivery. Raw GLFW imports are isolated under `GLFW_Vulkan.Raw`; application
code should use the typed packages instead.

