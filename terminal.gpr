project Terminal extends "terminal_glfw_vulkan_app/terminal_glfw_vulkan_app.gpr" is
   for Object_Dir use "obj";
   for Exec_Dir use "bin";

   package Builder is
      for Executable ("terminal_app_main.adb") use "terminal";
   end Builder;
end Terminal;
