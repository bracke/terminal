with "../terminal_common/terminal_common.gpr";

library project Terminal_PTY_POSIX is
   --  Small C shims (terminal_pty_posix_sys.c) let the platform headers resolve
   --  facilities whose values differ across Linux and macOS/BSD -- errno's
   --  accessor symbol and the TIOCSWINSZ/TIOCSCTTY ioctl request numbers.
   for Languages use ("Ada", "C");
   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Library_Dir use "lib";
   for Library_Name use "terminal_pty_posix";
   for Library_Kind use "static";

   package Compiler is
      for Default_Switches ("Ada") use ("-gnat2022", "-gnata", "-gnatwe");
   end Compiler;
end Terminal_PTY_POSIX;

